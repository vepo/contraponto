package dev.vepo.contraponto.postresponse;

public record PostResponseCardView(String title,
                                   String authorName,
                                   String excerpt,
                                   String url) {}
